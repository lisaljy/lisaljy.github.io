# Personal Site

An Astro static website for a personal homepage.

## Commands

```sh
npm install
npm run dev
npm run build
```

`npm install` downloads the project dependencies. `npm run dev` starts the local preview server. `npm run build` creates the static site in `dist/`.
